name: Notificação de Merge para o Discord
on:
  pull_request:
    types: [closed]
    branches:
      - main
jobs:
  notify:
    if: github.event.pull_request.merged == true
    runs-on: ubuntu-latest
    steps:
      - name: Enviar notificação para o Discord
        uses: sarisia/actions-status-discord@v1
        with:
          webhook: ${{ secrets.DISCORD_WEBHOOK }}
          title: "🚀 Novo Merge na Main!"
          description: |
            A branch **${{ github.event.pull_request.head.ref }}** foi mesclada à branch **main**.
            Lembrete para a equipe: façam um `git pull origin main` antes de continuar o trabalho.
